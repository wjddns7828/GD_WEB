--일정관리 게시판
CREATE TABLE CALENDARBOARD(
	SEQ NUMBER PRIMARY KEY,
	ID VARCHAR2(30) NOT NULL,
	TITLE VARCHAR(500) NOT NULL,
	CONTENT VARCHAR2(4000),
	MDATE VARCHAR2(12) NOT NULL,
	REGDATE DATE NOT NULL
);


INSERT INTO CALENDARBOARD
(SEQ, ID, TITLE, CONTENT, MDATE, REGDATE)
VALUES((SELECT NVL(MAX(SEQ),0)+1 FROM CALENDARBOARD c), 'RABIT', '일정관리게시판', '마지막 SERVLET 실습이다 야호', '202307241200', SYSDATE);

INSERT INTO CALENDARBOARD
(SEQ, ID, TITLE, CONTENT, MDATE, REGDATE)
VALUES((SELECT NVL(MAX(SEQ),0)+1 FROM CALENDARBOARD c), 'RABIT', '프로젝트모임', '프로젝트 주제를 정하는 모임 잘 정리해야함', '202307251200', SYSDATE);

INSERT INTO CALENDARBOARD
(SEQ, ID, TITLE, CONTENT, MDATE, REGDATE)
VALUES((SELECT NVL(MAX(SEQ),0)+1 FROM CALENDARBOARD c), 'RABIT', '즐거운 수업', '새로운 과목의 수업이 나간다고 한다', '202307251200', SYSDATE);

INSERT INTO CALENDARBOARD
(SEQ, ID, TITLE, CONTENT, MDATE, REGDATE)
VALUES((SELECT NVL(MAX(SEQ),0)+1 FROM CALENDARBOARD c), 'RABIT', '프로젝트 회의', '각 주제별 프로젝트 구성의 최종 단계 회의이다', '202307251200', SYSDATE);

INSERT INTO CALENDARBOARD
(SEQ, ID, TITLE, CONTENT, MDATE, REGDATE)
VALUES((SELECT NVL(MAX(SEQ),0)+1 FROM CALENDARBOARD c), 'RABIT', 'UI/UX회의', 'AJAX로 작업하자 AJAX 뭐지?', '202307251200', SYSDATE);

INSERT INTO CALENDARBOARD
(SEQ, ID, TITLE, CONTENT, MDATE, REGDATE)
VALUES((SELECT NVL(MAX(SEQ),0)+1 FROM CALENDARBOARD c), 'RABIT', 'API모듈 발표', '발표를 해야 하는데', '202307281200', SYSDATE);




SELECT SEQ ,ID ,TITLE ,CONTENT ,MDATE ,REGDATE 
	FROM CALENDARBOARD c 
	WHERE SUBSTR(MDATE,0,8) = '20230725';


SELECT SEQ,ID,TITLE,MDATE,REGDATE
	FROM(SELECT ROW_NUMBER() OVER(PARTITION BY SUBSTR(MDATE,1,8) ORDER BY MDATE)RN,
			SEQ,ID,TITLE ,MDATE ,REGDATE 
			FROM CALENDARBOARD c
			WHERE ID = 'RABIT'
			AND SUBSTR(MDATE,1,6)= '202307')
	WHERE RN BETWEEN 1 AND 3; 

SELECT SEQ,ID,TITLE,MDATE ,REGDATE 
	FROM CALENDARBOARD c 
		WHERE ID='RABIT' 
		AND SUBSTR(MDATE,1,8) ='20230725';
	
SELECT COUNT(*)
	FROM CALENDARBOARD c 
		WHERE ID='RABIT' 
		AND SUBSTR(MDATE,1,8) ='20230725';
	
SELECT SEQ,ID,TITLE,CONTENT,MDATE ,REGDATE 
	FROM CALENDARBOARD c 
		WHERE ID='RABIT' 
		AND SUBSTR(MDATE,1,8) ='20230725'
		AND SEQ =3;	

DELETE FROM CALENDARBOARD c 
	WHERE SEQ = '2';


















































































































