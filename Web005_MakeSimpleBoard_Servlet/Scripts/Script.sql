CREATE TABLE SIMPLEBOARD(
	SEQ VARCHAR2(10) PRIMARY KEY,
	WRITER VARCHAR2(20),
	TITLE VARCHAR2(50),
	CONTENT VARCHAR2(300),
	REGDATE DATE DEFAULT SYSDATE,
	DELFLAG CHAR
);

DROP TABLE SIMPLEBOARD;

CREATE SEQUENCE SIMPLEBOARD_SEQ START WITH 1 INCREMENT BY 1;

SELECT *
	FROM SIMPLEBOARD;
	
--새글 작성
INSERT INTO SIMPLEBOARD(SEQ,WRITER,TITLE,CONTENT,DELFLAG)
	VALUES (SIMPLEBOARD_SEQ.NEXTVAL,'동글','동글동글','동글동글합니다','N');
	
--전체글 조회
SELECT SEQ,TITLE,WRITER,REGDATE
		FROM SIMPLEBOARD
		WHERE DELFLAG = 'N'
			ORDER BY REGDATE DESC;
	
--상세조회
SELECT SEQ,TITLE,WRITER,CONTENT,REGDATE
	FROM SIMPLEBOARD
		WHERE TITLE = '네모글';

--글 수정 setUpdateBoard
UPDATE SIMPLEBOARD SET TITLE='몽글',CONTENT='몽글몽글합니다.'
	WHERE SEQ = '3';

--글 삭제 setDeleteBoard
UPDATE SIMPLEBOARD SET DELFLAG='Y'
	WHERE SEQ='3';





























